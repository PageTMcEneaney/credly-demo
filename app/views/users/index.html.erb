<section class="text-gray-600 body-font">
  <div class="container mx-auto flex px-5 md:flex-row flex-col items-center">
    <div class="lg:flex-grow md:w-1/2 lg:pr-24 md:pr-16 flex flex-col md:items-start md:text-left mb-16 md:mb-0 items-center text-center">
      <h1 class="title-font sm:text-4xl text-3xl mb-4 font-medium text-gray-900">Find <strong>verified</strong> heroes
        <br class="hidden lg:inline-block">
      </h1>
      <p class="mb-8 leading-relaxed">
        The year is 2030, and mankind has developed an AI which turned on its creators. A human
        resistance has been formed to defend humanity from the onslaught of AI and big data. We need
        your help to identify the best resistance fighters to aid in humanity&#39;s defense.
        <br/><strong>Find qualified heroes, and issue them badges using the Credly API.</strong>
      </p>
      <div class="flex justify-center">
        <button class="inline-flex text-white bg-orange-500 border-0 py-2 px-6 focus:outline-none hover:bg-orange-600 rounded text-lg">Add New Hero</button>
        <button class="ml-4 inline-flex text-gray-700 bg-gray-100 border-0 py-2 px-6 focus:outline-none hover:bg-gray-200 rounded text-lg">Random Hero</button>
      </div>
    </div>
    <div class="lg:max-w-lg lg:w-full md:w-1/2 w-5/6">
      <img class="object-cover object-center rounded-lg shadow" alt="hero" src="/credly-demo.jpeg">
    </div>
  </div>
  <div class="container px-5 mx-auto">
  <h1 class="text-3xl py-10">Active Users</h1>
  <div class="flex flex-wrap -m-2">
    <% @users.each do |user| %>
        <div class="p-2 lg:w-1/3 md:w-1/2 w-full">
          
            <div class="h-full flex items-center border-gray-800 border p-4 rounded-lg shadow hover:shadow-lg">
              <a class="my-auto" href=<%= user_path(user) %>>
                <img alt="team" class="w-16 h-16 bg-gray-100 object-cover object-center flex-shrink-0 rounded-full mr-4" src=<%= user.thumbnail %>><br/>
              </a>
              <div class="flex-grow">
                <h2 class="text-black title-font font-medium"><%= user.name %></h2>
                <p class="text-gray-600">Assigned Badges: 0 </p>
              </div>
              <div class="grow">
                <p>Add Badges:</p>
                <button 
                  class="inline-flex text-white bg-orange-500 border-0 py-2 px-5 focus:outline-none hover:bg-orange-600 rounded text-md" 
                  data-user=<%= user.id %> 
                  data-add-badge="1"
                  onclick="window.location = '/badge_one/index?id=<%= user.id %>'">
                  #1
                </button>
                <button 
                  class="ml-4 inline-flex text-gray-700 bg-gray-100 border-0 py-2 px-5 focus:outline-none hover:bg-gray-200 rounded text-md" 
                  data-user=<%= user.id %> 
                  data-add-badge="2"
                  onclick="window.location = '/badge_two/index?id=<%= user.id %>'">
                  #2
                </button>
              </div>
            </div>
          
        </div>
    <% end %>    
    </div>
  </div>
</section>
